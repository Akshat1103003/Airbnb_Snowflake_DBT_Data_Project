version: 2
models:
  - name: bronze_bookings
    config:
      materialized: incremental
  - name: bronze_listings
    config:
      materialized: table
  - name: bronze_hosts
    config:
      materialized: incremental